<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">   


<mapper namespace="com.admin.entity.OauthAuthorities">
      <!-- 自动生成id策略 -->
    <insert id="add" parameterType="com.admin.entity.OauthAuthorities" useGeneratedKeys="true" keyProperty="id">  
       	INSERT INTO oauth_authorities 
       	( username, authority) 
       	VALUES
       	( #{username}, #{authority})
    </insert>  
	
	<delete id="del" parameterType="com.admin.entity.OauthAuthorities">
		DELETE FROM oauth_authorities
	</delete>
	
	<update id="upd" parameterType="com.admin.entity.OauthAuthorities">
		UPDATE oauth_authorities SET
	           username = #{username}, 
	           authority = #{authority} 
	</update>
  	
	<resultMap type="com.admin.entity.OauthAuthorities" id="result">
        <result property="username" column="username"/>
        <result property="authority" column="authority"/>
	</resultMap>
  	
	<sql id="columns">
		t.username, t.authority 
	</sql>
	
	<sql id="order">
		<choose>
			<when test="@chok.devwork.Ognl@isNotEmpty(sort)">
				ORDER BY tt.<include refid="orderParse1"/> ${order}
			</when>
			<when test="@chok.devwork.Ognl@isNotEmpty(multiSort) and @chok.devwork.Ognl@isEmpty(sort)">
				ORDER BY <foreach collection="multiSort" index="index" item="item" separator=",">tt.<include refid="orderParse2" /> ${item.sortOrder}</foreach>  
			</when>
		</choose>
	</sql>
	<sql id="orderParse1">
		<if test="sort == 'username'">username</if>
		<if test="sort == 'authority'">authority</if>
	</sql>
	<sql id="orderParse2">
		<if test="item.sortName == 'username'">username</if>
		<if test="item.sortName == 'authority'">authority</if>
	</sql>
	
	<sql id="page">
		<if test="@chok.devwork.Ognl@isNotEmpty(offset) and @chok.devwork.Ognl@isNotEmpty(limit)"> LIMIT #{offset,javaType=String,jdbcType=NUMERIC}, #{limit,javaType=String,jdbcType=NUMERIC} </if>
	</sql>
	
	<sql id="dynamicWhere">
		<where>	      				
	       <if test="@chok.devwork.Ognl@isNotEmpty(username)"> AND t.username LIKE CONCAT('%',#{username},'%')</if>
	       <if test="@chok.devwork.Ognl@isNotEmpty(authority)"> AND t.authority LIKE CONCAT('%',#{authority},'%')</if>
	       <if test="@chok.devwork.Ognl@isNotEmpty(search)"> AND (t.username LIKE CONCAT('%',#{search},'%') OR t.authority LIKE CONCAT('%',#{search},'%'))</if>	
		</where>
	</sql>	
	
	<select id="get" resultMap="result">
		SELECT <include refid="columns"/> 
		  FROM oauth_authorities t 
	</select>
	
	<select id="query" resultType="list" resultMap="result">
		SELECT tt.* FROM 
		(
			SELECT <include refid="columns"/> FROM oauth_authorities t
			<include refid="dynamicWhere" />
		) tt
		<include refid="order" />
		<include refid="page" />
	</select>
	
	<select id="getCount" resultType="int">
		SELECT COUNT(1) FROM oauth_authorities t
		<include refid="dynamicWhere" />
	</select>
</mapper>  
